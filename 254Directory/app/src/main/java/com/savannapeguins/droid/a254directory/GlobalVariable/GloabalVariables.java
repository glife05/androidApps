package com.savannapeguins.droid.a254directory.GlobalVariable;

import java.util.UUID;

public class GloabalVariables {
    public static String emailInfo;//global variable to hold email variable
    public static String varUID= UUID.randomUUID().toString();//global variable for random unique ids for the listings
}
